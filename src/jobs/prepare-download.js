/**
 * Create a zip archive of requested file formats for a given track
 * includes high res artworks if applicable
 */

export const zipForDownload = async (job) => {
  try {
    return Promise.resolve()
  } catch (err) {
    return Promise.reject(err)
  }
}

export default zipForDownload
